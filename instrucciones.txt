

Cambios solicitados

Registro de categorías

Archivo: src/views/Wizard/steps/StepCategories.tsx

Ajustar la validación para que el usuario seleccione como mínimo 1 categoría (actualmente el mínimo es 3).

Sección “Mis Palabras”

Actualmente se encuentra en el menú lateral del Home.

Debe moverse a la pantalla de Configuración y renombrarse a “Configuración de mis palabras”.

Al ingresar, se debe mostrar una opción para activar/desactivar si las palabras creadas por el usuario aparecen en el Home (esta lógica ya existe, solo hay que verificarla).

Además, agregar un control de frecuencia de aparición con @react-native-community/slider, con tres niveles:

Normal

Alto

Muy alto

La frecuencia seleccionada definirá qué tan seguido aparecerán las palabras creadas por el usuario en el Home (dentro de home/index).

Importante: este algoritmo de frecuencia solo aplica a las palabras creadas por el usuario.

Las palabras que provienen de las categorías seleccionadas no se ven afectadas por esta configuración.